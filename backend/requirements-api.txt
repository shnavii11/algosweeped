# Slim runtime deps for the deployed web API (Render).
# Only what backend/app actually imports — the heavy scraper deps
# (playwright, google-generativeai/grpcio, supabase, alembic, pytest, bs4)
# live in requirements.txt and are NOT needed to serve the API.
fastapi==0.136.3
uvicorn[standard]==0.48.0
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.2.0
sqlalchemy[asyncio]==2.0.50
asyncpg==0.31.0
httpx==0.27.2
python-jose[cryptography]==3.5.0
redis[hiredis]==7.4.0
